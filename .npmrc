# Suppress deprecation warnings from nested dependencies
# These are from build tools (electron-builder) and don't affect runtime security
loglevel=error

# Force use of prebuilt binaries for better-sqlite3
build_from_source=false
